# About Review List module
This module provides following functionalities:

* Dynamically Creates Content type for Reviews and Articles on module install with basic fields (Title, Body, Image and Tag and Article ID(integer API ID)).
* Dynamically create view for showing list of Reviews with attached article.
* Programmaticly overrides Article title by fetching it from API based on Article ID.
